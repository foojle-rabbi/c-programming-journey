#include<stdio.h> 
#include<stdlib.h>

struct node{
    int data;
    struct node* left;
    struct node* right;
};

int main()
{
    // practice tree last koiya ekhono valo koira ei file ta complete e kori nai
    return 0;

}

    //     9
    //    / \
    //   5   10
    //  / \    \
    // 2   4    15

    // // kir >>> >> MM<< >> << .. >F>>> ? ?  ? ? ? ? ? ;;; ??? ?? >>>> >>> << >> *** 
    // // kir >>> >> MM<< >> << .. >F>>> ? ?  ? ? ? ? ? ;;; ??? ?? >>>> >>> << >> *** 
    // // kir >>> >> MM<< >> << .. >F>>> ? ?  ? ? ? ? ? ;;; ??? ?? >>>> >>> << >> *** 
    // // kir >>> >> MM<< >> << .. >F>>> ? ?  ? ? ? ? ? ;;; ??? ?? >>>> >>> << >> *** 
    // // kir >>> >> MM<< >> << .. >F>>> ? ?  ? ? ? ? ? ;;; ??? ?? >>>> >>> << >> *** 
    // vvvvvvvcccc
    // kir >>> >> MM<< >> << .. >F>>> ? ?  ? ? ? ? ? ;;; ??? ?? >>>> >>> << >> *** v